{
  // Typecheck the dogfood test suite (also dogfoods that the
  // @rangojs/router/testing* TYPES resolve cleanly from a consumer).
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["node", "react", "react-dom"],
    "noEmit": true
  },
  "include": ["test/**/*", "vitest.config.ts", "vitest.rsc.config.ts"]
}
