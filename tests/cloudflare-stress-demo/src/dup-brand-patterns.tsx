/** The other half of the same-staticPrefix pair — see dup-cat-patterns.tsx. */
import { makeDupGroup } from "./stress/chain-factories.js";

const dupBrandPatterns = makeDupGroup("dup-brand", "brandPage", "brand-page");

export default dupBrandPatterns;
