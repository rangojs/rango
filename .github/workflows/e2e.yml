name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * *"

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

# Container /etc/hosts orders ::1 before 127.0.0.1, and Node 20+ defaults
# DNS resolution to verbatim OS order. fetch('http://localhost') then
# tries IPv6 first, but vite dev / preview bind to 127.0.0.1 only, so
# waitForReady in the e2e fixture times out before Happy Eyeballs falls
# back to IPv4. Forcing ipv4first eliminates the slow fallback. Harmless
# for non-container jobs.
env:
  NODE_OPTIONS: --dns-result-order=ipv4first

jobs:
  # On push to main, wait 2 minutes before starting CI. Combined with
  # cancel-in-progress, a follow-up push within the window cancels this
  # run so only the latest commit gets a full suite. Instant for PRs.
  debounce:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Wait for additional pushes
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: sleep 120

  lint:
    needs: [debounce]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v5

      # Containerised jobs run as root while the workspace files are owned by
      # the runner user, which makes git refuse ("dubious ownership") and
      # breaks the lefthook install in the pnpm prepare script. Harmless on
      # non-container jobs.
      - name: Mark workspace safe for git
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Run oxlint
        run: pnpm lint

      - name: Check e2e dev/prod bucketing
        run: pnpm check:e2e-bucketing

  format:
    needs: [debounce]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v5

      # Containerised jobs run as root while the workspace files are owned by
      # the runner user, which makes git refuse ("dubious ownership") and
      # breaks the lefthook install in the pnpm prepare script. Harmless on
      # non-container jobs.
      - name: Mark workspace safe for git
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Check formatting
        run: pnpm format

  typecheck:
    needs: [debounce]
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v5

      # Containerised jobs run as root while the workspace files are owned by
      # the runner user, which makes git refuse ("dubious ownership") and
      # breaks the lefthook install in the pnpm prepare script. Harmless on
      # non-container jobs.
      - name: Mark workspace safe for git
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Build rangojs-router
        run: pnpm --filter @rangojs/router build

      - name: Run typecheck
        run: pnpm typecheck

  unit-tests:
    needs: [debounce]
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v5

      # Containerised jobs run as root while the workspace files are owned by
      # the runner user, which makes git refuse ("dubious ownership") and
      # breaks the lefthook install in the pnpm prepare script. Harmless on
      # non-container jobs.
      - name: Mark workspace safe for git
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Build rangojs-router
        run: pnpm --filter @rangojs/router build

      - name: Run unit tests
        run: pnpm --filter @rangojs/router test:unit

  vite-rsc-demo-e2e:
    needs: [smoke-e2e]
    if: |
      (github.event_name == 'pull_request' && github.event.pull_request.draft == false) ||
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'schedule'
    runs-on: ubuntu-latest
    # Playwright image ships browsers + system deps; skip ~15min apt install
    # per job. Tag must match the @playwright/test version in pnpm-lock.
    container: mcr.microsoft.com/playwright:v1.57.0-jammy
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - project: dev
            shard: 1
          - project: dev
            shard: 2
          # Production tests run in single job (build-once via setup project)
          - project: production

    steps:
      - uses: actions/checkout@v5

      # Containerised jobs run as root while the workspace files are owned by
      # the runner user, which makes git refuse ("dubious ownership") and
      # breaks the lefthook install in the pnpm prepare script. Harmless on
      # non-container jobs.
      - name: Mark workspace safe for git
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Build rangojs-router
        run: pnpm --filter @rangojs/router build

      - name: Run demo app e2e tests (${{ matrix.project }}${{ matrix.shard && format(' shard {0}', matrix.shard) || '' }})
        run: |
          if [ -n "${{ matrix.shard }}" ]; then
            pnpm --filter vite-rsc-demo exec playwright test --project=${{ matrix.project }} --shard=${{ matrix.shard }}/2
          else
            pnpm --filter vite-rsc-demo exec playwright test --project=${{ matrix.project }}
          fi

      - name: Upload test results
        uses: actions/upload-artifact@v6
        if: failure()
        with:
          name: playwright-report-demo-${{ matrix.project }}${{ matrix.shard && format('-shard-{0}', matrix.shard) || '' }}
          path: tests/vite-rsc-demo/playwright-report/
          retention-days: 7

  smoke-e2e:
    needs: [debounce]
    if: |
      github.event_name != 'pull_request' ||
      github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    container: mcr.microsoft.com/playwright:v1.57.0-jammy
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5

      # Containerised jobs run as root while the workspace files are owned by
      # the runner user, which makes git refuse ("dubious ownership") and
      # breaks the lefthook install in the pnpm prepare script. Harmless on
      # non-container jobs.
      - name: Mark workspace safe for git
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Build rangojs-router
        run: pnpm --filter @rangojs/router build

      - name: Run smoke tests
        run: pnpm --filter @rangojs/router test --project=smoke

      - name: Upload test results
        uses: actions/upload-artifact@v6
        if: failure()
        with:
          name: playwright-report-smoke
          path: packages/rangojs-router/playwright-report/
          retention-days: 7

  rangojs-router-e2e:
    needs: [smoke-e2e]
    if: |
      (github.event_name == 'pull_request' && github.event.pull_request.draft == false) ||
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'schedule'
    runs-on: ubuntu-latest
    container: mcr.microsoft.com/playwright:v1.57.0-jammy
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        include:
          # Dev tests sharded across 3 jobs
          - project: dev
            shard: 1
            shardTotal: 3
          - project: dev
            shard: 2
            shardTotal: 3
          - project: dev
            shard: 3
            shardTotal: 3
          # Production tests sharded across 2 jobs
          - project: production
            shard: 1
            shardTotal: 2
          - project: production
            shard: 2
            shardTotal: 2
          # HMR tests run serially (workers=1) to avoid filesystem interference.
          # hmr-routes (route-types-hmr.test.ts) is intentionally absent: it is
          # test.skip(isCI)'d because the file watcher is unreliable on GitHub
          # Actions' virtualized FS, so the job ran zero tests. Run it locally:
          # pnpm --filter @rangojs/router exec playwright test --project=hmr-routes
          - project: hmr-client
            workers: 1
          - project: hmr-loader
            workers: 1
          - project: hmr-intercept
            workers: 1

    steps:
      - uses: actions/checkout@v5

      # Containerised jobs run as root while the workspace files are owned by
      # the runner user, which makes git refuse ("dubious ownership") and
      # breaks the lefthook install in the pnpm prepare script. Harmless on
      # non-container jobs.
      - name: Mark workspace safe for git
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Build rangojs-router
        run: pnpm --filter @rangojs/router build

      - name: Run rangojs-router e2e tests (${{ matrix.project }}${{ matrix.shard && format(' shard {0}', matrix.shard) || '' }})
        env:
          CHOKIDAR_USEPOLLING: ${{ matrix.workers && 'true' || '' }}
        run: |
          WORKERS_FLAG=""
          if [ -n "${{ matrix.workers }}" ]; then
            WORKERS_FLAG="--workers=${{ matrix.workers }}"
          fi
          # HMR projects use useFixture() to start their own dev server,
          # so skip dependency projects (dev, build, warmup) that would
          # re-run hundreds of unrelated tests and blow the timeout.
          # POSIX case — container jobs default to sh, which doesn't
          # support bash's [[ ... == pattern ]].
          NO_DEPS_FLAG=""
          case "${{ matrix.project }}" in
            hmr-*) NO_DEPS_FLAG="--no-deps" ;;
          esac
          if [ -n "${{ matrix.shard }}" ]; then
            pnpm --filter @rangojs/router test --project=${{ matrix.project }} --shard=${{ matrix.shard }}/${{ matrix.shardTotal }} $WORKERS_FLAG $NO_DEPS_FLAG
          else
            pnpm --filter @rangojs/router test --project=${{ matrix.project }} $WORKERS_FLAG $NO_DEPS_FLAG
          fi

      - name: Upload test results
        uses: actions/upload-artifact@v6
        if: failure()
        with:
          name: playwright-report-router-${{ matrix.project }}${{ matrix.shard && format('-shard-{0}', matrix.shard) || '' }}
          path: packages/rangojs-router/playwright-report/
          retention-days: 7

  cloudflare-basic-e2e:
    needs: [smoke-e2e]
    if: |
      (github.event_name == 'pull_request' && github.event.pull_request.draft == false) ||
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'schedule'
    runs-on: ubuntu-latest
    container: mcr.microsoft.com/playwright:v1.57.0-jammy
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v5

      # Containerised jobs run as root while the workspace files are owned by
      # the runner user, which makes git refuse ("dubious ownership") and
      # breaks the lefthook install in the pnpm prepare script. Harmless on
      # non-container jobs.
      - name: Mark workspace safe for git
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Build rangojs-router
        run: pnpm --filter @rangojs/router build

      - name: Run cloudflare-basic e2e tests
        run: pnpm --filter cloudflare-basic test:e2e
        env:
          TEST_DEBUG: "1"

      - name: Upload test results
        uses: actions/upload-artifact@v6
        if: failure()
        with:
          name: playwright-report-cloudflare-basic
          path: tests/cloudflare-basic/playwright-report/
          retention-days: 7

  cloudflare-multi-router-e2e:
    needs: [smoke-e2e]
    if: |
      (github.event_name == 'pull_request' && github.event.pull_request.draft == false) ||
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'schedule'
    runs-on: ubuntu-latest
    container: mcr.microsoft.com/playwright:v1.57.0-jammy
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v5

      # Containerised jobs run as root while the workspace files are owned by
      # the runner user, which makes git refuse ("dubious ownership") and
      # breaks the lefthook install in the pnpm prepare script. Harmless on
      # non-container jobs.
      - name: Mark workspace safe for git
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Build rangojs-router
        run: pnpm --filter @rangojs/router build

      - name: Run cloudflare-multi-router e2e tests
        run: pnpm --filter cloudflare-multi-router test:e2e
        env:
          TEST_DEBUG: "1"

      - name: Upload test results
        uses: actions/upload-artifact@v6
        if: failure()
        with:
          name: playwright-report-cloudflare-multi-router
          path: examples/cloudflare-multi-router/playwright-report/
          retention-days: 7

  react-experimental-e2e:
    needs: [smoke-e2e]
    if: |
      (github.event_name == 'pull_request' && github.event.pull_request.draft == false) ||
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'schedule'
    runs-on: ubuntu-latest
    container: mcr.microsoft.com/playwright:v1.57.0-jammy
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v5

      # Containerised jobs run as root while the workspace files are owned by
      # the runner user, which makes git refuse ("dubious ownership") and
      # breaks the lefthook install in the pnpm prepare script. Harmless on
      # non-container jobs.
      - name: Mark workspace safe for git
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Build rangojs-router
        run: pnpm --filter @rangojs/router build

      - name: Run react-experimental e2e tests
        run: pnpm --filter react-experimental test:e2e

      - name: Upload test results
        uses: actions/upload-artifact@v6
        if: failure()
        with:
          name: playwright-report-react-experimental
          path: tests/react-experimental/playwright-report/
          retention-days: 7

  no-typescript-e2e:
    needs: [smoke-e2e]
    if: |
      (github.event_name == 'pull_request' && github.event.pull_request.draft == false) ||
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'schedule'
    runs-on: ubuntu-latest
    container: mcr.microsoft.com/playwright:v1.57.0-jammy
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v5

      # Containerised jobs run as root while the workspace files are owned by
      # the runner user, which makes git refuse ("dubious ownership") and
      # breaks the lefthook install in the pnpm prepare script. Harmless on
      # non-container jobs.
      - name: Mark workspace safe for git
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Build rangojs-router
        run: pnpm --filter @rangojs/router build

      - name: Run no-typescript e2e tests
        run: pnpm --filter no-typescript test:e2e

      - name: Upload test results
        uses: actions/upload-artifact@v6
        if: failure()
        with:
          name: playwright-report-no-typescript
          path: tests/no-typescript/playwright-report/
          retention-days: 7

  cloudflare-stress-demo-e2e:
    needs: [smoke-e2e]
    if: |
      (github.event_name == 'pull_request' && github.event.pull_request.draft == false) ||
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'schedule'
    runs-on: ubuntu-latest
    container: mcr.microsoft.com/playwright:v1.57.0-jammy
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v5

      # Containerised jobs run as root while the workspace files are owned by
      # the runner user, which makes git refuse ("dubious ownership") and
      # breaks the lefthook install in the pnpm prepare script. Harmless on
      # non-container jobs.
      - name: Mark workspace safe for git
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Build rangojs-router
        run: pnpm --filter @rangojs/router build

      - name: Run cloudflare-stress-demo e2e tests
        run: pnpm --filter cloudflare-stress-demo test:e2e

      - name: Upload test results
        uses: actions/upload-artifact@v6
        if: failure()
        with:
          name: playwright-report-cloudflare-stress-demo
          path: tests/cloudflare-stress-demo/playwright-report/
          retention-days: 7
